#include<iostream>
#include<stdlib.h>
using namespace std;
short RandomNumber(short From, short To) {
	return rand() % (To - From + 1) + From;
}
void FillArrayWithRandomNumbers(short Array[100], short&ArrayLength) {

	cout << "\nEnter Number of elements\n";
	cin >> ArrayLength;
	for (short  i = 0; i < ArrayLength; i++)
	{
		Array[i]= RandomNumber(1,100);
	}
}
void PrintArray(short Array[100], short ArrayLength) {

	for (short i = 0; i < ArrayLength; i++)
	{
		cout << Array[i] << " ";
	}
	cout << endl;
}
short SumArray(short Array[100], short ArrayLength) {
	short Sum=0;
    for (short  i = 0; i < ArrayLength; i++)
	{
		Sum += Array[i];
	
	}
	return Sum;
}
int main() {

	srand((unsigned)time(NULL));
	short Array[100], ArrayLength = 0;
	FillArrayWithRandomNumbers(Array, ArrayLength);
	cout << "\nArray Elements:";
	PrintArray(Array, ArrayLength);
	cout << "\nSum of all number is : " << SumArray(Array, ArrayLength)<<endl;

	return 0;
}
